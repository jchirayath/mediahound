# wiki/

The MediaHound wiki pages, kept **in-repo** so docs live with the code.

These mirror the GitHub wiki (published from the separate `mediahound.wiki.git`
repo). Treat the copy here as the source of truth; to publish edits to the
GitHub wiki, push them to that wiki repo (or wire up a sync workflow — ask if
you want one added).

Pages: Home · Adding-Media · Command-Reference · Configuration-and-API-Keys ·
Desktop-App · Editing-and-Persisting-Changes · Privacy · Publishing-Your-Catalog
(+ `_Sidebar`).
